<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="sbc_secuencias.list">
      <field name="name">sbc_secuencias list</field>
      <field name="model">sbc_secuencias.sbc_secuencias</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="sbc_secuencias.action_window">
      <field name="name">sbc_secuencias window</field>
      <field name="res_model">sbc_secuencias.sbc_secuencias</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="sbc_secuencias.action_server">
      <field name="name">sbc_secuencias server</field>
      <field name="model_id" ref="model_sbc_secuencias_sbc_secuencias"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="sbc_secuencias" id="sbc_secuencias.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="sbc_secuencias.menu_1" parent="sbc_secuencias.menu_root"/>
    <menuitem name="Menu 2" id="sbc_secuencias.menu_2" parent="sbc_secuencias.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="sbc_secuencias.menu_1_list" parent="sbc_secuencias.menu_1"
              action="sbc_secuencias.action_window"/>
    <menuitem name="Server to list" id="sbc_secuencias" parent="sbc_secuencias.menu_2"
              action="sbc_secuencias.action_server"/>
    -->
  </data>
</odoo>